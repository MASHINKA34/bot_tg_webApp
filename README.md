# Clicker Game - Telegram Web App

## 🎮 Описание
Telegram Web App кликер-игра с фермами, ежедневными бонусами и лидербордом.

## 📁 Структура проекта
```
clicker_webapp/
├── backend/
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── clicker.py      # Эндпоинты кликера
│   │   ├── daily.py        # Ежедневные бонусы
│   │   ├── farms.py        # Система ферм
│   │   └── leaderboard.py  # Лидерборд
│   ├── __init__.py
│   ├── config.py           # Конфигурация
│   ├── database.py         # Подключение к БД
│   ├── models.py           # SQLAlchemy модели
│   └── schemas.py          # Pydantic схемы
├── frontend/
│   ├── index.html          # HTML интерфейс
│   ├── styles.css          # Стили
│   └── app.js              # JavaScript логика
├── bot.py                  # Telegram бот
├── main.py                 # FastAPI приложение
├── .env                    # Переменные окружения
└── requirements.txt        # Зависимости
```

## 🔧 Установка

1. **Установи зависимости:**
```bash
pip install -r requirements.txt
```

2. **Настрой .env файл:**
```env
BOT_TOKEN=your_bot_token
WEBAPP_URL=your_ngrok_url
DATABASE_URL=sqlite+aiosqlite:///./clicker.db
SECRET_KEY=your_secret_key
```

3. **Запусти ngrok:**
```bash
ngrok http 8000
```

4. **Обнови WEBAPP_URL в .env** с ngrok URL

## 🚀 Запуск

### Запуск FastAPI сервера:
```bash
python main.py
```

### Запуск Telegram бота (в другом терминале):
```bash
python bot.py
```

## ✅ Исправленные ошибки

### 1. **Структура проекта**
- ✅ Создана правильная структура `backend/routers/`
- ✅ Добавлены `__init__.py` файлы
- ✅ Исправлены импорты в `main.py`

### 2. **Логирование**
- ✅ Включены логи в консоль
- ✅ Добавлен middleware для логирования всех HTTP запросов
- ✅ Логи показывают: метод, путь, статус, время выполнения

### 3. **Ошибка 422 (Unprocessable Entity)**
**Причина:** Неправильная структура JSON в запросе

**Решение:** Убедись что в `app.js` запросы отправляются так:
```javascript
await fetch(`${API_URL}/clicker/click`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ 
        telegram_id: userId,  // Обязательно
        clicks: clicksToSync   // Обязательно
    })
});
```

### 4. **Ошибка 404 (Not Found)**
**Причина:** Пользователь не создан в БД

**Исправлено:** В `clicker.py` добавлена автоматическая регистрация:
```python
if not user:
    user = User(
        telegram_id=request.telegram_id,
        balance=0,
        total_clicks=0,
        click_level=1,
        click_power=settings.CLICK_VALUE
    )
    db.add(user)
```

## 📊 Пример логов

Теперь в консоли ты увидишь:
```
2025-11-08 15:04:08,269 - __main__ - INFO - 🎮 Clicker Web App запускается на http://0.0.0.0:8000
2025-11-08 15:04:08,298 - __main__ - INFO - 🚀 Запуск приложения...
2025-11-08 15:04:08,344 - __main__ - INFO - ✅ База данных инициализирована
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
2025-11-08 15:04:15,123 - __main__ - INFO - GET /api/clicker/stats/123456 - 200 - 0.045s
2025-11-08 15:04:17,891 - __main__ - INFO - POST /api/clicker/click - 200 - 0.023s
```

## 🐛 Дебаг

Если твой друг получает ошибки:

1. **Проверь ngrok URL** - он должен быть актуальным
2. **Проверь telegram_id** - в `app.js` строка:
```javascript
const userId = tg.initDataUnsafe?.user?.id || 123456;
```

3. **Проверь логи** - теперь все запросы логируются
4. **Проверь БД** - файл `clicker.db` должен создаваться автоматически

## 📱 Тестирование

1. Открой бота в Telegram
2. Нажми `/start`
3. Нажми кнопку "🎮 Открыть игру"
4. Смотри логи в консоли где запущен `python main.py`

## 🎯 API Endpoints

- `POST /api/clicker/click` - Обработка кликов
- `GET /api/clicker/stats/{telegram_id}` - Статистика игрока
- `POST /api/clicker/upgrade` - Улучшение клика
- `POST /api/daily/claim/{telegram_id}` - Забрать ежедневный бонус
- `GET /api/farms/{telegram_id}` - Получить фермы
- `POST /api/farms/buy` - Купить ферму
- `POST /api/farms/collect/{farm_id}` - Собрать с фермы
- `GET /api/leaderboard/` - Топ-10 игроков

## 🔥 Фичи

- ⚡ Батчинг кликов (синхронизация каждые 5 секунд)
- 🏭 Пассивный доход с ферм (с AFK лимитом 2 часа)
- 🎁 Ежедневные бонусы со стриками
- 🏆 Лидерборд топ-10
- 💾 Автосохранение при закрытии приложения
